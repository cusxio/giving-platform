// Import { EGHL_URL } from '#/envvars'
// Import { http, HttpResponse } from 'msw'

export const handlers = [
  // Http.get(EGHL_URL, ({ request }) => {
  //   // const url
  //   Console.log('hello')
  // }),
]
